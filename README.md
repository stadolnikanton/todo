# 📝 To-Do Web Application

Веб-приложение для управления задачами с системой регистрации и аутентификации пользователей. Проект реализован на Python с использованием Flask и следует архитектурному паттерну MVC (Model-View-Controller).

## 🚀 Возможности

- ✅ Регистрация и аутентификация пользователей
- ✅ Создание, просмотр, редактирование и удаление задач
- ✅ Отметка задач как выполненных
- ✅ Персональный профиль пользователя
- ✅ Современный и адаптивный пользовательский интерфейс
- ✅ Система уведомлений (flash messages)

## 🛠 Технологический стек

- **Python 3.12+** - основной язык программирования
- **Flask 3.1.2+** - веб-фреймворк
- **SQLAlchemy 2.0+** - ORM для работы с базой данных
- **Alembic 1.16.5+** - миграции базы данных
- **Jinja2** - шаблонизатор (встроен в Flask)
- **uv** - современный пакетный менеджер для Python
- **PostgreSQL/SQLite** - база данных (поддерживаются обе)

## 📋 Требования

- Python 3.12 или выше
- uv (пакетный менеджер)
- PostgreSQL (опционально, по умолчанию используется SQLite)

## 🔧 Установка и настройка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd todo
```

### 2. Установка зависимостей

Установите зависимости через `uv`:

```bash
uv sync
```

Это автоматически создаст виртуальное окружение и установит все необходимые пакеты.

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите необходимые параметры:

```env
# URL базы данных
# Для PostgreSQL:
DB_URL=postgresql://username:password@localhost:5432/todo_db
# Для SQLite (по умолчанию):
# DB_URL=sqlite:///todo.db

# Секретный ключ для сессий Flask (опционально, генерируется автоматически)
SECRET_KEY=your-secret-key-here
```

**Примечание:** Если `DB_URL` не указан, приложение автоматически использует SQLite базу данных `todo.db` в корне проекта.

### 4. Применение миграций базы данных

Инициализируйте и примените миграции Alembic:

```bash
# Создание начальной миграции (если еще не создана)
alembic revision --autogenerate -m "Initial migration"

# Применение всех миграций
alembic upgrade head
```

### 5. Запуск приложения

Запустите сервер разработки:

```bash
python main.py
```

Или используйте Flask CLI:

```bash
flask run
```

Приложение будет доступно по адресу: `http://localhost:5000`

## 📁 Структура проекта

```
todo/
├── alembic/              # Миграции базы данных
│   ├── versions/         # Файлы миграций
│   ├── env.py           # Конфигурация Alembic
│   └── script.py.mako   # Шаблон для миграций
├── controller/          # Контроллеры (бизнес-логика)
│   ├── task.py          # Контроллер задач
│   └── user.py          # Контроллер пользователей
├── model/               # Модели базы данных
│   ├── base.py          # Базовый класс моделей
│   ├── models.py        # Импорт всех моделей
│   ├── task.py          # Модель задачи
│   └── user.py          # Модель пользователя
├── view/                # Представления (маршруты Flask)
│   ├── auth.py          # Маршруты аутентификации
│   ├── task.py          # Маршруты задач
│   └── user.py          # Маршруты пользователя
├── templates/           # HTML шаблоны
│   ├── auth/            # Шаблоны авторизации
│   ├── todo/            # Шаблоны задач
│   ├── user/            # Шаблоны профиля
│   ├── base.html        # Базовый шаблон
│   └── index.html       # Главная страница
├── static/              # Статические файлы
│   └── css/
│       └── style.css    # Стили приложения
├── db.py                # Конфигурация базы данных
├── main.py              # Точка входа приложения
├── alembic.ini          # Конфигурация Alembic
├── pyproject.toml       # Зависимости проекта
└── README.md            # Документация
```

## 🎯 Использование

### Регистрация нового пользователя

1. Перейдите на главную страницу
2. Нажмите "Зарегистрироваться"
3. Заполните форму:
   - Имя
   - Email
   - Пароль (минимум 8 символов)
   - Подтверждение пароля

### Вход в систему

1. Нажмите "Войти"
2. Введите email и пароль
3. После успешного входа вы будете перенаправлены на главную страницу

### Работа с задачами

#### Создание задачи

1. Нажмите кнопку "+ Новая задача"
2. Заполните название задачи (обязательно)
3. Добавьте описание (необязательно)
4. Нажмите "Добавить задачу"

#### Просмотр задачи

- Нажмите на название задачи в списке для просмотра деталей

#### Отметка задачи как выполненной

- Нажмите на иконку галочки рядом с задачей
- Или на странице деталей задачи нажмите "Отметить как выполненное"

#### Удаление задачи

- Нажмите на иконку корзины рядом с задачей
- Или на странице деталей задачи нажмите "Удалить задачу"

### Просмотр профиля

- Нажмите на иконку меню в левом верхнем углу для перехода к профилю

## 🔐 Безопасность

- Пароли хранятся в хешированном виде (SHA-256)
- Проверка прав доступа к задачам (пользователь может работать только со своими задачами)
- Защита маршрутов от неавторизованного доступа
- Валидация входных данных

## 🧪 Разработка

### Создание новой миграции

```bash
alembic revision --autogenerate -m "Описание изменений"
alembic upgrade head
```

### Откат миграции

```bash
alembic downgrade -1
```

### Просмотр текущей версии БД

```bash
alembic current
```

## 📝 Модели данных

### User (Пользователь)

- `id` - уникальный идентификатор
- `name` - имя пользователя
- `email` - email (уникальный)
- `password` - хеш пароля
- `register_date` - дата регистрации

### Task (Задача)

- `id` - уникальный идентификатор
- `owner_id` - ID владельца (связь с User)
- `name` - название задачи
- `description` - описание задачи
- `created_at` - дата создания
- `is_completed` - статус выполнения (true/false)

## 🐛 Решение проблем

### Ошибка подключения к базе данных

- Проверьте правильность `DB_URL` в файле `.env`
- Убедитесь, что база данных создана и доступна
- Для PostgreSQL проверьте права доступа пользователя

### Ошибки миграций

```bash
# Просмотр истории миграций
alembic history

# Применение конкретной миграции
alembic upgrade <revision>
```

### Проблемы с зависимостями

```bash
# Переустановка зависимостей
uv sync --reinstall
```

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 👨‍💻 Автор

Разработано как учебный проект для изучения Flask и веб-разработки на Python.

## 🔄 Планы на будущее

- [ ] Редактирование задач
- [ ] Фильтрация задач (выполненные/невыполненные)
- [ ] Поиск по задачам
- [ ] Сортировка задач
- [ ] Категории задач
- [ ] Сроки выполнения задач
- [ ] Экспорт задач
- [ ] API для мобильного приложения

---

**Приятного использования!** 🎉
